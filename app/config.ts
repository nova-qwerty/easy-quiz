import config from "./config.json";

export const logoUrl = config.logoUrl;
export const titleImgUrl = config.titleImgUrl;
export const arrowImgUrl = config.arrowImgUrl;
export const backgroundImage = config.backgroundImage;
export const backgroundColor = config.backgroundColor;
export const steps = config.steps;
export const footer = config.footer;
export const result = config.result;